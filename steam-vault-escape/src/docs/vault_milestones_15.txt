Steam Vault Escape - Milestone Plan v1.5
Implementation Phases
This plan breaks down the v1.5 implementation into manageable milestones for Cline to execute sequentially.

---

MILESTONE 0: Foundation Refactor (v1.0 → v1.5 Bridge) ✅ COMPLETE
Goal: Prepare the codebase for v1.5 by removing obsolete systems, adding new APIs, and updating core data structures.

Tasks:

1. ✅ Remove Vault Controller
   - File: lib/constants.ts
     * Delete VAULT_CONTROLLER constant
   - File: lib/vault-logic.ts
     * Remove getVaultController() function
     * Remove references to 'vault-controller' in classifyGame()
   - File: app/page.tsx
     * Remove all Vault Controller initialization code
     * Remove from featured game logic

2. ✅ Add API Routes for Metadata
   - File: app/api/steamspy/route.ts (NEW)
     ✅ Create SteamSpy API route
     ✅ Fetch rating (positive/negative ratio)
     ✅ Fetch median_forever for hours-to-beat estimate
     ✅ Fetch tags for categorization
   
   - File: app/api/steam-store/route.ts (NEW)
     ✅ Create Steam Store API route
     ✅ Fetch Metacritic score (game.metacritic?.score)
     ✅ Fetch recommendations count as fallback rating

3. ✅ Update Type Definitions
   - File: types/steam.ts
     * Add metacritic?: number
     * Add hoursTobeat?: number (from SteamSpy median_forever)
     * Add tags?: string[]
     * Add recommendations?: number
   
   - File: types/vault.ts
     * Remove GameState type (replaced by pool membership)
     * Add new types:
       ```typescript
       export type GameTier = 'cheap' | 'moderate' | 'epic';
       
       export interface GameProgress {
         currentPower: number;
         maxPower: number;
         isDrained: boolean;
         lastPlaytime: number;
       }
       
       export interface ShopSlot {
         appId: number | null;
         tier: GameTier | null;
       }
       ```
     * Update VaultState interface:
       ```typescript
       export interface VaultState {
         // Dual currency
         collectionPower: number;
         liberationKeys: number;
         
         // Three pools (arrays of app IDs)
         pool1_unlocked: number[];
         pool2_hidden: number[];
         pool3_keyGames: number[];
         
         // Shop state
         shopSlots: ShopSlot[];
         
         // Game progress tracking
         gameProgress: { [appId: number]: GameProgress };
         
         // Metadata
         lastSync: number;
         steamId: string;
         version: string;
       }
       ```

4. ✅ Update Formulas & Constants
   - File: lib/constants.ts
     ✅ Remove old formulas (unlockCost, passiveRate, clickValue, liberationBonus)
     ✅ Add new v1.5 formulas:
       ```typescript
       export const FORMULAS = {
         // Unlock cost: Metacritic × Hours to Beat
         unlockCost: (metacritic: number, hoursTobeat: number): number => 
           Math.floor(metacritic * hoursTobeat),
         
         // Click value: Steam hours played
         clickValue: (steamMinutes: number): number => 
           Math.floor(steamMinutes / 60),
         
         // Max power: Click value × 100
         maxPower: (clickValue: number): number => 
           clickValue * 100,
         
         // Refresh cost: Metacritic ÷ 10 (rounded up)
         refreshCost: (metacritic: number): number => 
           Math.ceil(metacritic / 10),
         
         // Key reward: Metacritic score (or fallback)
         keyReward: (metacritic: number): number => 
           metacritic || 70,
         
         // Tier classification
         getTier: (unlockCost: number): GameTier => {
           if (unlockCost < 1000) return 'cheap';
           if (unlockCost < 3000) return 'moderate';
           return 'epic';
         }
       };
       ```
     * Add new thresholds:
       ```typescript
       export const THRESHOLDS = {
         KEY_GAME_MIN_PLAYTIME: 30, // Minutes
         DRAW_COST: 10, // Keys
         REDRAW_COST: 5, // Keys
         STARTING_GAME_METACRITIC_MIN: 70,
         STARTING_GAME_METACRITIC_MAX: 79,
         STARTING_GAME_HOURS_MIN: 5,
         STARTING_GAME_HOURS_MAX: 10,
         DEFAULT_METACRITIC: 70,
         DEFAULT_HOURS_TO_BEAT: 30,
       };
       ```

5. ✅ Update Storage Structure
   - File: lib/storage.ts
     ✅ Update StoredState interface to match new VaultState
     ✅ Update saveToStorage() to handle dual currency
     ✅ Update loadFromStorage() to handle new structure
     ✅ Add version migration logic (v1.0 saves should be cleared)

6. ✅ Create Utility Functions
   - File: lib/game-utils.ts (NEW)
     ✅ Function: enrichGameWithMetadata(game: SteamGame): Promise<SteamGame>
       - Fetch Metacritic from Steam Store API
       - Fetch hours-to-beat from SteamSpy
       - Calculate unlock cost
       - Determine tier
       - Return enriched game
     
     ✅ Function: selectStartingGame(games: SteamGame[]): SteamGame | null
       - Filter: Metacritic 70-79, 5-10 hours played
       - Fallback: Any game with 5-10 hours played
       - Fallback: Lowest playtime game
       - Return selected game or null

7. ✅ Audit Existing Code
   - Keep (reusable):
     ✅ Steam API integration (app/api/steam-library/route.ts)
     ✅ Image handling (lib/steam-images.ts)
     ✅ Virtual scrolling implementation
     ✅ localStorage patterns
     ✅ Loading/error states
     ✅ Dev panel
   
   - Modify:
     🔧 app/page.tsx (complete UI restructure for 3 sections)
     🔧 lib/vault-logic.ts (new pool-based logic)
     🔧 types/* (new interfaces)
   
   - Remove:
     ❌ Featured game clicking system
     ❌ Single click card
     ❌ Vault Controller references
     ❌ Victory detection (moves to Milestone 8)
     ❌ Old passive income system

8. Test Foundation
   - Verify API routes work:
     * Test SteamSpy route with a game
     * Test Steam Store route with a game
     * Verify Metacritic scores retrieved
     * Verify hours-to-beat calculated
   
   - Verify formulas:
     * Calculate unlock costs for sample games
     * Verify tier classification
     * Test refresh cost calculation
   
   - Verify storage:
     * Save new structure to localStorage
     * Load and verify data integrity
     * Confirm version migration clears old saves

Success Criteria:

✅ Vault Controller removed from codebase
✅ SteamSpy & Steam Store API routes functional
✅ Type definitions updated for v1.5
✅ New formulas implemented and tested
✅ Storage structure updated
✅ Starting game selection logic created
✅ No TypeScript errors
✅ Foundation ready for Milestone 1

---

MILESTONE 1: Three-Pool Foundation ✅ COMPLETE
Goal: Establish the core three-pool system and data structures.
Tasks:

✅ Create Pool Manager
   - File: lib/pool-manager.ts
   ✅ Implement initializePools(), promoteKeyGame(), unlockGame() functions
   ✅ Logic for selecting starting game (70s Metacritic, 5-10 hours)
   ✅ Helper functions: getGamesInPool(), needsPoolInitialization(), getPoolStats()

✅ Update Save Data Structure
   - File: lib/storage.ts
   ✅ VaultState interface with three pool arrays
   ✅ shopSlots array added
   ✅ gameProgress map added
   ✅ saveToStorage() and loadFromStorage() updated

✅ Initialize Pools on Load
   - File: app/page.tsx
   ✅ On Steam library fetch, call initializePools()
   ✅ Store pools in VaultState
   ✅ Save to localStorage

✅ Test Pool Initialization
   ✅ Verified starting game selected correctly (Sid Meier's Pirates!)
   ✅ Verified Pool 1 has 1 game
   ✅ Verified Pool 2 has 239 remaining played games  
   ✅ Verified Pool 3 has 314 never-played games
   ✅ Console logs confirm proper pool distribution

Success Criteria:

✅ Three pools correctly populated
✅ Starting game meets criteria
✅ Pools saved to localStorage


MILESTONE 2: Shop System with 5 Slots ✅ COMPLETE
Goal: Replace single-click card with 5-slot shop displaying locked games.
Tasks:

✅ Create Shop Manager
   - File: lib/shop-manager.ts
   ✅ ShopManager with initializeShop(), categorizeByTier(), drawNewGame()
   ✅ Weighted tier distribution implemented

✅ Calculate Unlock Costs
   - File: lib/constants.ts
   ✅ FORMULAS.unlockCost(metacritic, hoursTobeat)
   ✅ FORMULAS.getTier() for tier classification

✅ Update UI Layout
   - File: app/page.tsx
   ✅ Removed single click card
   ✅ Added "Unlock with Collection Power" section
   ✅ 5 shop slots displayed horizontally
   ✅ Each slot shows: Game art, name, Metacritic, unlock cost
   ✅ "Unlock" button (disabled if not enough power)

✅ Implement Unlock Logic
   - File: app/page.tsx
   ✅ handleShopUnlock() function
   ✅ Checks collectionPower >= unlockCost
   ✅ Deducts power
   ✅ Moves game Pool 2 → Pool 1
   ✅ Empties shop slot
   ✅ Saves to localStorage

✅ Display Empty Slots
   ✅ Empty slot shows: "🔒 Spend 10 Keys to Draw"
   ✅ Disabled if liberationKeys < 10
   ✅ Click opens Draw UI (for Milestone 4)

Success Criteria:
✅ 5 shop slots display locked games
✅ Unlock costs calculated correctly
✅ Unlock button works
✅ Games move from shop to Game Library
✅ Empty slots displayed correctly


MILESTONE 3: Game Library with Click & Drain
Goal: Display unlocked games, implement click mechanics, and drain system.
Tasks:

Create Game Library Section

File: src/app/page.tsx
Add "Game Library" section above shop
Display all Pool 1 games as clickable cards
Sort by click value (highest first)
Each card shows: Game art, name, click value, progress bar


Implement Click Handler

File: src/lib/click-manager.ts
Class: ClickManager
Method: handleClick(appId, game, progress)
Calculate power gained = Math.floor(playtime_forever / 60)
Check if drained: currentPower >= maxPower
Return: { powerGained, isDrained }


Update Game Progress

On click:

Add powerGained to collectionPower
Update gameProgress[appId].currentPower
If drained, set gameProgress[appId].isDrained = true
Save to localStorage
Re-render card




Display Progress Bars

Each game card shows progress: currentPower / maxPower
Visual progress bar: ████████░░░░░
Text: "4,250 / 15,000" below progress bar


Drained State

When isDrained = true:

Card shows dark overlay
Display: "⚠️ DRAINED"
Calculate refresh cost: Math.ceil(metacritic / 10) keys
Show "Refresh for X 🔑" button
Clicking is disabled




Refresh Logic

On "Refresh" click:

Check if liberationKeys >= refreshCost
Deduct keys
Reset: currentPower = 0, isDrained = false
Save to localStorage
Re-render card





Success Criteria:

✅ Game Library displays Pool 1 games
✅ Clicking generates power
✅ Progress bars update correctly
✅ Games drain at correct threshold
✅ Drained state displays correctly
✅ Refresh button works


MILESTONE 4: Draw New Game UI ✅ COMPLETE
Goal: Implement slot machine interface for drawing games from Pool 2.
Tasks:

✅ Create Draw Modal Component
   - File: app/page.tsx (integrated modal)
   ✅ Modal overlay with 3 card backs
   ✅ Card backs are clickable
   ✅ User clicks one → reveal animation
   ✅ Shows drawn game details
   ✅ "Accept" or "Redraw for 5 🔑" buttons

✅ Implement Draw Logic
   - File: lib/draw-manager.ts
   ✅ drawGameFromPool2() with weighted tier targeting
   ✅ getSlotTargetTier() for tier distribution
   ✅ canAffordDraw() and canAffordRedraw() helpers

✅ Connect to Empty Slots
   ✅ Empty slot click triggers handleDrawSlot()
   ✅ Checks if liberationKeys >= 10
   ✅ Opens DrawModal with pre-drawn game
   ✅ Card back click reveals game (handleCardReveal)
   ✅ Deducts 10 keys on reveal
   ✅ Fills shop slot with drawn game

✅ Redraw Logic
   ✅ "Redraw" button checks liberationKeys >= 5
   ✅ Deducts 5 keys per redraw
   ✅ Calls drawGameFromPool2() again
   ✅ Shows new game reveal
   ✅ Can redraw unlimited times

✅ Weighted Draw Implementation
   ✅ 70% chance for target tier, fallback to others
   ✅ Tier pattern: [cheap, moderate, cheap, epic, moderate]
   ✅ Console logging for debugging
   ✅ Weighted targeting verified

Success Criteria:
✅ Empty slot opens Draw Modal
✅ 3 card backs display
✅ Clicking card reveals game
✅ Drawn game fills shop slot
✅ Redraw works and costs 5 keys
✅ Weighted targeting works correctly


MILESTONE 5: Key Games Section ✅ COMPLETE
Goal: Display all never-played games with key rewards, click to launch Steam.
Tasks:

✅ Create Key Games Section
   - File: app/page.tsx (integrated section)
   ✅ "⭐ Key Games" section below Game Library
   ✅ Grid layout (2-5 columns responsive)
   ✅ All Pool 3 games displayed
   ✅ Full game art with overlay
   ✅ Key value shown (🔑 {metacritic} Keys)

✅ Calculate Key Values
   ✅ Key value = Metacritic score
   ✅ Display as: "🔑 87 Keys"
   ✅ Default to 70 if no Metacritic

✅ Click to Launch Steam
   ✅ On card click: window.open(`steam://store/${appId}`, '_blank')
   ✅ Launches Steam app to game's store page
   ✅ Console logging for debugging

✅ Visual Design
   ✅ Full-color game art (not locked/darkened)
   ✅ Purple border (border-purple-500)
   ✅ Sort by Metacritic (highest first), then alphabetically
   ✅ Hover effects: scale + shadow
   ✅ Info overlay with game name + key value
   ✅ Instruction text: "Click any game to launch it in Steam. Play 30+ minutes to earn keys!"
   ✅ Tab system implemented (Game Library + Key Games tabs)
   ✅ Old v1 UI elements removed

Success Criteria:
✅ All Pool 3 games displayed (~314 games)
✅ Key values shown correctly
✅ Clicking opens Steam app
✅ Grid layout responsive
✅ Sorted by value (highest keys first)
✅ Duplicate draw bug fixed
✅ Game Library sorted by click value


MILESTONE 6: Key Game Detection & Rewards ✅ COMPLETE
Goal: Detect when player has played a Key Game, award keys, move to Pool 2.
Tasks:

✅ Create Detection System
   - File: lib/key-game-detector.ts
   ✅ detectNewlyPlayedKeyGames() function
   ✅ Compare current Steam playtime vs cached playtime
   ✅ If playtime >= 30 minutes (crossed threshold), game detected
   ✅ calculateTotalKeysAwarded() helper
   ✅ shouldSync() for periodic checking

✅ Award Keys on Library Refresh
   - File: app/page.tsx - handleRefresh()
   ✅ Call detectNewlyPlayedKeyGames()
   ✅ Award keys = Metacritic score (or 70 default)
   ✅ Move games Pool 3 → Pool 2
   ✅ Auto-refresh all drained games FREE (autoRefreshAllDrained())
   ✅ Show celebration alert

✅ Auto-Refresh All Drained
   - File: lib/click-manager.ts
   ✅ autoRefreshAllDrained() already implemented
   ✅ Loops through gameProgress
   ✅ Resets isDrained and currentPower for all drained games
   ✅ Free refresh (no key cost)

✅ Celebration Notification
   ✅ Alert modal shows game names + keys awarded
   ✅ Format: "🎉 KEY GAME COMPLETED!\n\nHades (+93 🔑)\n\n✅ All drained games refreshed FREE!"
   ✅ Supports multiple games detected at once

Success Criteria:
✅ Playing Key Game detected correctly
✅ Keys awarded = Metacritic score
✅ Game moves Pool 3 → Pool 2
✅ Celebration notification shown
✅ All drained games auto-refresh FREE


MILESTONE 7: UI Polish & Animations ✅ MOSTLY COMPLETE
Goal: Add visual polish, animations, and improved UX.
Tasks:

✅ Card Reveal Animation
   - DrawModal uses `flip-card` class
   - 3D card backs with gradient background
   - Hover effects: scale + shadow (`hover:scale-105`, `hover:shadow-2xl`)
   - Smooth reveal transitions

✅ Progress Bar Animation
   - Smooth width transition with `transition-all`
   - Color shift based on remaining power:
     * < 20%: Red (`bg-red-500`)
     * 20-50%: Yellow (`bg-yellow-500`)  
     * > 50%: Green (`bg-green-500`)
   - Animated countdown number centered in bar

✅ Tier Visual Differentiation
   - Cheap games: Gray border (`border-gray-400`) + `tier-cheap` class
   - Moderate games: Blue border (`border-blue-500`) + `tier-moderate` class
   - Epic games: Gold border (`border-vault-gold`) + `tier-epic` class
   - Custom glow effects per tier in CSS

✅ Hover States
   - Game cards: Scale up on hover (`hover:scale-105`)
   - Buttons: Scale + brightness (`hover:scale-110`, `hover:bg-*`)
   - Smooth transitions (200-300ms via `transition-all`)
   - Opacity changes for overlays (`hover:opacity-100`)

✅ Empty Slot Visual
   - Dashed border for empty slots (`border-dashed border-vault-gold/30`)
   - Lock icon centered with `animate-pulse`
   - `pulse-glow` class for attention-grabbing effect
   - Hover border color change

✅ Drained Game Visual
   - Dark overlay (50% opacity - `bg-black/70`)
   - Grayscale filter on game art (`grayscale`)
   - Warning icon (⚠️) and "DRAINED" text
   - Refresh button prominently displayed

✅ Loading States
   - ✅ Loading spinner on initial fetch (animate-pulse gear icon)
   - ✅ Button disabled states during operations
   - ✅ Skeleton loaders for game cards implemented:
     * Featured game skeleton (300x450px placeholder)
     * Shop section skeleton (5 card placeholders)
     * Library section skeleton (12 card placeholders)
     * Gradient backgrounds with pulse animation
   - ✅ All skeletons use proper aspect ratios

✅ Responsive Design  
   - ✅ Grid uses responsive columns (`grid-cols-2 md:grid-cols-4 lg:grid-cols-6`)
   - ✅ Shop grid adapts (`md:grid-cols-3 lg:grid-cols-5`)
   - ✅ Key Games grid: `grid-cols-2 md:grid-cols-3 lg:grid-cols-4 xl:grid-cols-5`
   - ✅ Mobile-friendly padding and spacing
   - ✅ Touch-friendly button sizes (px-8 py-4 for tabs)
   - ✅ Responsive text sizes (text-lg to text-3xl scaling)



Success Criteria:

✅ Animations smooth and performant
✅ Tiers visually distinct
✅ Hover states engaging
✅ Loading states clear
✅ Mobile-friendly layout


MILESTONE 8: Testing & Bug Fixes
Goal: Comprehensive testing and bug fixes before deployment.
Test Cases:

Pool Transitions

 Starting game selected correctly
 Unlocking moves game Pool 2 → Pool 1
 Playing Key Game moves Pool 3 → Pool 2
 No games appear in multiple pools


Shop Mechanics

 Initial shop has correct tier distribution
 Unlock costs calculated correctly
 Unlocking empties slot
 Draw fills empty slot with correct tier
 Redraw gives different game


Click & Drain

 Clicks generate correct power
 Progress bars update correctly
 Games drain at correct threshold
 Refresh costs calculated correctly
 Manual refresh works


Key Game Detection

 Playing game for 30+ min detected
 Keys awarded = Metacritic score
 Game moves to Pool 2
 Auto-refresh triggers
 Celebration notification shows


Edge Cases

 Pool 2 empty: Draw shows "No games available"
 Pool 3 empty: Key Games shows "All games played!"
 Not enough keys: Buttons disabled
 Not enough power: Unlock disabled
 Private Steam profile: Error message


localStorage

 Save/load works correctly
 Pools persist across sessions
 Progress persists correctly
 Cache expiration works


Performance

 Large libraries (500+ games) load quickly
 Clicking is responsive (no lag)
 Draw UI smooth
 No memory leaks



Bug Fixes:

Fix any issues discovered during testing
Handle edge cases gracefully
Improve error messages
Optimize performance bottlenecks

Success Criteria:

✅ All test cases pass
✅ No critical bugs
✅ Performance acceptable
✅ Error handling robust


MILESTONE 9: Deployment & Documentation
Goal: Deploy to Vercel and finalize documentation.
Tasks:

Environment Setup

Verify STEAM_API_KEY in Vercel environment variables
Test production build locally: npm run build
Check for TypeScript errors


Deploy to Vercel

Push to GitHub main branch
Verify auto-deploy triggers
Test on live URL: steam-vault-escape.vercel.app


User Documentation

File: README.md
How to use the app
How to find Steam ID
How the three pools work
How to earn keys


Developer Documentation

Code comments for complex logic
Architecture overview
How to add new features
Testing instructions


Post-Launch Monitoring

Check Vercel logs for errors
Monitor user feedback
Track usage metrics (if available)



Success Criteria:

✅ App deployed and accessible
✅ No errors in production
✅ Documentation complete
✅ Ready for user testing


MILESTONE 10: Future Enhancements (Post-v1.5)
Goal: Plan and implement additional features based on user feedback.
Potential Features:

Passive Key Generation

Earn 1 key per hour of total library playtime
Incentivizes playing anything, not just Key Games


Daily Challenges

"Play a horror game today" → Bonus keys
"Unlock 3 games" → Bonus power multiplier


Collection Milestones

Unlock all RPGs → Permanent +10% power bonus
Play 50 Key Games → Unlock special tier


Bulk Operations

"Refresh All Drained" button (costs total refresh cost)
"Draw 5 Games" button (costs 50 keys, fills all empty slots)


Friend Leaderboards

Compare unlock progress with Steam friends
"Who played more Key Games this week?"


Prestige System

Reset all progress for permanent bonuses
Prestige levels with cosmetic rewards


Enhanced Analytics

Graphs of progress over time
Genre breakdown of library
Cost-per-hour analysis



Implementation: Based on user feedback and priorities after v1.5 launch.
